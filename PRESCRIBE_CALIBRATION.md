# 族六处方层的标定记录：四个失败的构造

族六只留下了一个能用的处方。这份文档记录**被否掉的四个构造**，
因为它们决定了族六为什么长成现在这样 —— 也因为其中三个是量化研究里
常见的做法，值得写清楚它们在真实面板上是怎么坏的。

复现环境：真实 A 股面板 800 只 / 2015-2026 / 2787 交易日，
月频 126 期，`D:/huang/.../ashare_historical_daily_2015_2026.parquet`。
下述数字来自 `_probe_*.py` 系列脚本（未随包分发，见文末）。

---

## ① 无约束换手 Oracle：没有判别力

**构造**：给定策略实际花掉的换手预算 τ_t，问「任何权重向量最多能拿到多少」。
这是一个可解析求解的重排问题 —— 从 r 最低的持仓卖出、买入 r 最高的标的，
`R*(τ)` 在 τ 上分段线性且凹（实测一阶差递减：1.032 → 0.989 → 0.952 → …）。

**为什么否掉**：上界离任何真实策略都无穷远，三个版本被压成同一个数。

| 版本 | 你的增量 | Oracle 增量 | 捕获率 |
|---|---|---|---|
| careless（三处漏口） | +0.386%/期 | +71.68%/期 | **0.54%** |
| careful | −0.115%/期 | +73.96%/期 | **−0.16%** |
| random | −0.159%/期 | +75.71%/期 | **−0.21%** |

捕获率全在 ±0.5% 以内 —— 一个连「植入三处前视漏口」和「随机选股」
都区分不开的量，不能进审计工具。

**为什么筛选层能用而组合层不能**：Oracle 上界要有意义，决策空间必须被
制度锁死（等额、固定 q、只换名单）。组合层 800 只的连续权重没有这个锁，
完美预知太强。

---

## ② 无交易带宽扫描：教科书做法，实测是纯噪声

**构造**：`|Δw_i| < b` 的标的不动，其余交易到目标。扫 b 找净收益最优。

**结果看起来极好**：最优 b=12%，换手 96% → 25%，净年化 −0.60% → **+2.96%**，
全样本 **+356bp**（单边成本 20bp 假设下）。

**为什么否掉**：在**随机选股**组合上调同一个旋钮：

| 组合 | 最优带宽 | 全样本改善 | 半样本 OOS |
|---|---|---|---|
| 本账 careful | 12.0% | +356bp | +449bp |
| random7 | 8.0% | +245bp | +83bp |
| random21 | 12.0% | +218bp | **−1518bp** |
| random99 | 12.0% | **+705bp** | +932bp |
| random123 | 8.0% | +290bp | −359bp |
| random512 | 8.0% | +398bp | −345bp |

随机组合的改善均值 +371bp、范围 [+218, +705]bp —— 本账的 +356bp
**完整落在噪声区间内**，且随机组合的半样本 OOS 从 −1518bp 到 +932bp。

**这是族六存在的全部理由**：一个不带闸门的优化建议器会理直气壮地
推荐这个带宽，还附上 +356bp 的漂亮数字。所以工具现在报
「不可处方，最小可检出改善约 700bp/年」。

---

## ③ 交叉成本按阈值带宽算：τ 不单调 ⇒ 在噪声里挑最小值

**构造**：`c*(b) = [G(0) − G(b)] / [2·(τ(0) − τ(b))·ppy]`，
报「c* 最低的那个带宽」。

**为什么否掉**：阈值带宽下 τ(b) **不单调**：

| 带宽 b | 0.5% | 1% | 2% | 3% | 5% | 8% | 12% |
|---|---|---|---|---|---|---|---|
| τ | 95% | 94% | 93% | **61%** | **87%** | 35% | 25% |
| c* | −42.0 | 15.4 | 100.5 | −13.3 | 272.4 | 10.0 | −0.2 |

b=3% 给 τ=61%、b=5% 反而 87% —— 于是「c* 最低的带宽」是在一列
上下乱跳的数里取最小值。本账 −42.0bp，随机对照 [−46.1, −17.8]bp，同量级。

**修法**：不用阈值，用预算份额 φ 直接削，让 τ(φ) 在**构造上**单调。

---

## ④ 按 |Δw| 排序削预算：把组合身份换掉了

**构造**：每期把 |Δw| 从大到小执行，直到用掉 φ 份额预算。τ(φ) 这样确实单调了。

**为什么否掉**：削预算不是省成本，是换成了另一个组合。

| φ | 最大权重 | 有效注数 1/Σw² | 与目标名单重合 | 毛年化 |
|---|---|---|---|---|
| 1.0 | 18.4% | 14.8 | 100% | +4.17% |
| 0.8 | 17.0% | 18.7 | 63% | +4.66% |
| 0.6 | 17.1% | 17.9 | 43% | +3.76% |
| **0.4** | **95.7%** | **1.5** | **1%** | **+21.37%** |
| 0.3 | 92.7% | 1.8 | 1% | +22.67% |
| 0.2 | 48.9% | 4.5 | 2% | −10.12% |

φ=0.4 那个 **+21.37%** 是**一只股票**。c* 的分母（省下的换手）是干净的，
分子（毛收益之差）被身份漂移彻底污染。

**这与族四漏传权重是同一个病**：对照组每多变一个东西，结论就多一个混淆源。
修法也一样 —— 把身份钉住：

```
名单变动  进场/出场          ⇒ 必须全执行（身份由此钉住）
权重微调  两边都持有的名字   ⇒ 受预算份额 φ 约束
```

修好后名单重合恒为 100%、τ(φ) 单调，两条都是**算术保证而非实证发现**，
测试按恒等式断言（`test_identity_controlled_keeps_names`、`test_tau_monotone_in_phi`）。

---

## 留下来的那一个：身份受控前沿

修好之后，`reversal30` 上的结论是**旋钮不存在**：削权重微调只能把换手从
96% 省到 95%（1pp），因为它的换手 95% 是名单换血。这本身是该报的事实。

阳性对照证明闸门不是摆设 —— 市值前 200 只市值加权（微调占 **73%**）：

| φ | 换手 | 毛年化 | 逐期毛差 | 配对 t | 后半样本 |
|---|---|---|---|---|---|
| 1.0（当前） | 5.54% | +3.49% | — | — | — |
| 0.8 | 4.77% | +3.47% | −0.2bp | −0.93 | +0.2bp |
| 0.6 | 4.06% | +3.52% | +0.2bp | +0.34 | +0.9bp |
| 0.4 | 3.41% | +3.45% | −0.4bp | −0.40 | +0.4bp |
| 0.2 | 2.71% | +3.61% | +0.8bp | +0.46 | +3.9bp |
| **0.0** | **1.61%** | **+4.03%** | **+3.8bp** | +0.73 | +9.8bp |

换手省 71% 而毛年化从 +3.49% 升到 +4.03% ⇒ 交叉成本为**负**：
换手更低**且**毛收益更高，**在任何成本水平下都严格占优**，不需要费率假设。

**归因（关D）**：本账 +3.8bp/期，同规模随机对照 +0.4bp [−1.3, +3.2]bp。
超出随机范围，所以报告说这与选股有关；但同类市值加权组合普遍适用的部分
必须写成结构性结论，不许算成 alpha。

---

## 门槛 20% 的标定

真实面板扫 12 种形态（持仓 30/100/200 × 市值/等权 × 固定名单/反转换名）：

| 形态 | 总换手 | 微调占 | 削到 φ=0 省下 |
|---|---|---|---|
| 30 只 市值 固定名单 | 2.85% | 95% | 90% |
| 30 只 市值 反转换名 | 96.07% | 1% | 1% |
| 30 只 等权 固定名单 | 2.22% | 90% | 80% |
| 30 只 等权 反转换名 | 94.32% | 1% | −0% |
| 100 只 市值 固定名单 | 3.64% | 93% | 90% |
| 100 只 市值 反转换名 | 88.51% | 3% | 3% |
| 100 只 等权 固定名单 | 3.09% | 88% | 82% |
| 100 只 等权 反转换名 | 85.33% | 1% | 0% |
| 200 只 市值 固定名单 | 3.96% | 93% | 90% |
| 200 只 市值 反转换名 | 77.84% | 7% | 5% |
| 200 只 等权 固定名单 | 3.32% | 89% | 83% |
| 200 只 等权 反转换名 | 72.31% | 2% | 0% |

门槛之下 6 种形态削到 φ=0 平均只省 **1%** 换手（拒绝是对的），
门槛之上平均省 **86%**。**微调占比在 7% 与 88% 之间是空档** ——
门槛落在空档里，所以判别不依赖它的具体取值，挪 ±10pp 不改变任何裁决。

---

## 与文献的关系

| 工作 | 做什么 | 与族六的分界 |
|---|---|---|
| Novy-Marx & Velikov (2016) | buy/hold sS 规则用进出场阈值降换手 | 它改变**名单变动**的数量；族六刻意不碰名单，只削权重微调 |
| Detzel, Novy-Marx & Velikov (JF 2023) | 净成本下的资产定价模型比较 | 模型比较，不是给单个组合的可执行处方 |
| Jensen, Kelly, Malamud & Pedersen (RFS) | 把交易成本放进优化目标求可实现前沿 | 给**更好的组合**；族六给「你这个组合的哪个旋钮可以动」，且带过拟合闸门 |
| Perold (1988) | 实施缺口 | 谱系。族六隔离出其中一个成分：为权重微调付的钱 |

族六不声称提出了新的组合优化方法。它的增量是**把「这条建议能不能给」
做成一道可复现的闸门**，并把闸门拒绝的情形也当成结论报出来 ——
上面的②就是这道闸门唯一存在的理由。

## 复现

探针脚本不随包分发（依赖本地面板），保存在 `_retired/probes_prescribe/`：

| 脚本 | 对应结论 |
|---|---|
| `_probe_oracle.py` | ① 无约束 Oracle 无判别力 |
| `_probe_turnover_value.py` | 换手边际价值的判别力 + 同换手随机对照 |
| `_probe_band.py` | ② 带宽扫描是噪声 |
| `_probe_frontier.py` / `_frontier2.py` | ③ τ 不单调 |
| `_probe_why.py` | ④ 身份漂移死因确认 |
| `_probe_frontier3.py` | 身份受控构造 |
| `_probe_positive.py` | 阳性对照 cap200 |
| `_probe_gate.py` | 四关闸门标定 |
| `_probe_calib.py` | 20% 门槛的空档标定 |
| `_probe_report.py` | 端到端报告渲染 |

不依赖本地面板的部分已全部钉成回归测试（`tests/test_prescribe.py`，19 项）。

# SPEC v0.2：执行口径、参数邻域体检、推断项升级

> 立场：**全部离线**。不内置数据源、不联网、不上传 —— README 承诺不变。
> 需要外部事实（涨跌停标志、退市名单、第二家供应商信号）的检查，
> 一律做成**契约里的可选输入列**，由用户自己补。补了就审，没补就明确说
> 「这一项没查过」——不是查过通过了。

标定依据：本 SPEC 每条都由 `D:/desk/top1_h3_trade_details_v1341_v1348.xlsx`
（796 笔 / 2020-01 ~ 2026-08 / Top1 主力净流入 / 次日开盘买 / 持有 3 交易日）
实测过，数字写在各条的「实测」里。反例来源见 `PRESCRIBE_CALIBRATION.md`。

---

## 0. 为什么要有这一版：v0.1 在这份表上暴露的三个缺陷

这份表是**平台正常导出件**，用户操作没有错。v0.1 跑出来 0 BLOCK / 6 WARN，
但过程中暴露的问题全在工具侧：

### 缺陷① 契约没有执行价槽位 ⇒ 对账退化成恒等式

`detect.py:39` 的价格别名是 `close/px/price/vwap/settle`，**没有 open、
没有入场时点**。这份策略是「次日开盘买、退出日收盘卖」，为了塞进契约
必须把买入日那格 close 覆盖成合成的后复权开盘价：

```
hfq_open(买入日) = 次日开盘价 × hfq_close(卖出日) / 退出日收盘价
```

于是工具重算的每笔收益是

```
hfq_close(卖出日) / hfq_open(买入日) = 退出日收盘价 / 次日开盘价 ≡ xlsx 毛收益率
```

**外部价格在代数上整个约掉了。** 实测 784/796 笔吻合到 `<1e-9`
（余下 12 笔是买卖日撞同一格、覆盖顺序打乱）。所以当时写下的
「已用后复权价验证，误差 ~0.015%」**不是验证，是恒等式**。

同一个覆盖还污染了下游：涨跌停判定读同一张面板的 `pct_change`，
买入日那格已被换成开盘价。干净面板 9058 个涨停 / 覆盖后 9008 个。

### 缺陷② 对账项把恒等式报成 OK

这份 xlsx 是**闭合代数体**，实测：

| 关系 | 实测 |
|---|---|
| 月度汇总 `总净盈亏` = 交易明细逐笔加总（80 个月） | max\|diff\| = **0.0000** |
| `毛收益率` = 退出日收盘价 / 次日开盘价 − 1（796 笔） | max\|diff\| = **5.6e-17** |

净值从明细算出、明细收益从两个价格算出。**「自报净值对账」在这类
平台导出件上不可能失败**——打印的净值和重算它的分子分母是同一批数。
v0.1 对此报 OK，等于把恒等式说成通过。

### 缺陷③ 生存者检查在稀疏面板上假阳性

`lookahead.py` 的 `check_universe_survivorship` 读
`pm.loc[[last]].dropna(axis=1)`。第一版价格表只有每只票买/卖两天的价格，
最后一天只有 1 只票有价 ⇒ **313/314 只被判「已退市」**（我复现过）。
这是面板稀疏度造出来的数字，与策略无关。

---

## 1. 契约扩展：执行口径（解缺陷①，同时解接口痛点）

### 1.1 现状代价

一个 xlsx → 3 个 CSV 用掉 4 个脚本、2 次返工、41MB 中间件，
其中 `fix_entry_open.py` 整个存在的理由就是绕开「契约没有开盘价」。
这不是文档问题，是契约少了一维。

### 1.2 新增：价格面板可选列

```
date, code, close, [open], [amount], [up_limit], [down_limit], [is_suspended], [is_st]
```

`detect.py` 的 `_ALIAS` 增加：

```python
"open":       ("open", "open_price", "openprice", "o", "开盘价", "开盘"),
"up_limit":   ("up_limit", "uplimit", "limit_up", "涨停价", "涨停"),
"down_limit": ("down_limit", "downlimit", "limit_down", "跌停价", "跌停"),
"is_suspended": ("is_suspended", "suspended", "suspend", "停牌", "是否停牌"),
"is_st":      ("is_st", "st", "st_flag", "是否st"),
```

### 1.3 新增：`execution=` 参数

```python
sa.audit(weights, prices, nav,
         execution={"entry": "open", "exit": "close", "signal_lag": 1})
```

| 键 | 取值 | 默认 | 含义 |
|---|---|---|---|
| `entry` | `"close"` / `"open"` | `"close"` | 入场用哪个价 |
| `exit` | `"close"` / `"open"` | `"close"` | 出场用哪个价 |
| `signal_lag` | int ≥ 0 | `0` | 信号日到执行日的交易日间隔 |

`core.py` 的 `_seg_gross` 按此取价：`entry="open"` 时分母取 `open` 列
（**不是**被覆盖过的 close），`signal_lag` 用于对齐检查而非改变收益。

**实测**：这份表 `signal_lag=1`（796/796 笔信号日→买入日恰好 1 个交易日），
`延迟退出天数` 794/796 为 0 ⇒ 执行口径高度规整，值得有专门槽位。

### 1.4 硬要求：缺 open 列时不许自造

`entry="open"` 但价格面板无 `open` 列 ⇒ **skip 该项并说明**，
不许回退到 close 静默计算，更不许由用户在外部合成后塞进 close。
后者正是缺陷①的成因。

---

## 2. 对账项加「来源同一性」前置断言（解缺陷②）

### 2.1 判据

对账前先算：自报净值与重算净值的**逐期残差量级**。

```
resid = |own_ret - recomputed_ret|
若 max(resid) < 1e-9  ⇒  同源
```

### 2.2 三档输出

| 残差量级 | 结论 | 级别 |
|---|---|---|
| `max < 1e-9` | **本项不可审：同源** | `SKIP`（不是 OK） |
| `1e-9 ~ 阈值` | 正常对账 | 现有逻辑 |
| `> 阈值` | 现有 BLOCK/WARN | 现有逻辑 |

同源时报文必须写明：

> 你交的净值与按权重+价格重算的结果在浮点精度内完全一致
> （max 残差 X.Xe-NN）。这说明两者由**同一批数字**算出——
> 平台导出件通常如此。**本项因此不可审**，不是审过通过了。
> 要让这一项有内容，需要一份**独立来源**的价格面板。

### 2.3 为什么必须是 SKIP 而不是 OK

这是全项目的核心立场（`capability.py` 开头已写明「不能审必须和审过通过
严格区分」）。把恒等式报成 OK，用户会以为「对账过了」，而这一项什么都
没查。**v0.1 在这份真实客户件上正是这么报的。**

---

## 3. 生存者检查加面板密度门（解缺陷③）

进入 `check_universe_survivorship` 前：

```python
last_cov = pm.loc[pm.index.max()].notna().mean()
if last_cov < 0.5:
    rep.skip("股票池生存者偏差",
             f"末日价格覆盖率仅 {last_cov:.1%}（{n_alive}/{n_cols}）"
             f"—— 面板过于稀疏，无法区分「已退市」与「面板没这天的数」")
    return
```

**实测**：稀疏面板末日覆盖率 1/314 = 0.3% ⇒ 会输出「313 只已退市」；
完整日频面板覆盖率 314/314 = 100% ⇒ 正确输出「全部存活，无法判定」。

另：现有「无法判定」分支（全部存活且面板不含消失标的）保留，
但增加可选输入 `delisted=` 让用户补退市名单（见 §5）。

---

## 4. 新增族七：参数邻域体检（只体检，不寻优）

### 4.1 立场（不可协商）

**只回答一个问题：你声明的这档参数，是不是被过拟合出来的。**
即使邻域里有更好的档也**不推荐换档**。

理由在 `PRESCRIBE_CALIBRATION.md` §② 已标定：无交易带宽扫描在
本账给出 +356bp 的漂亮改善，但在随机选股组合上同一旋钮给出
+218 ~ +705bp（均值 +371），本账完整落在噪声内；半样本 OOS
从 −1518bp 到 +932bp。**一个不带闸门的建议器就是过拟合机器。**
族七通过「不给建议」在构造上免除这个风险。

### 4.2 输入要求

用户必须**显式声明**参数当前值，工具不猜：

```python
sa.audit(..., params={"holding_days": 3, "top_n": 1})
```

未声明 ⇒ 整族 skip。**不许从数据里反推参数再扫**（那是拿结果选搜索空间）。

### 4.3 检查 ①：持有期邻域配对检验（交易级，不是调仓级）

对每个 `H' ≠ H`，在**同一批交易事件、同一入场价**上算收益，做配对 t 检验。

这里的“交易”严格指 trade-level 事件，而不是 `weights.iterrows()` 的调仓日组合：

`H` 的定义是**含入场日计 1**：H=1 的退出日就是入场日，H=3 的退出索引为
入场日之后第 2 个交易日（`entry_pos + H - 1`）。这必须与交易明细的
`entry_date/exit_date` 语义一致。

- 有交易明细时使用 `entry_date/code`，可选 `exit_date`；当前 H 档可用实际 `exit_date`，邻域 H' 从同一事件重算。
- 没有交易明细时，从权重面板自动识别每个标的的 `0 → 正权重` 持仓段起点，每个起点是一笔交易事件。
- 不得把调仓日整行权重当成一笔交易收益；现金不能稀释交易级收益，也不得把混龄持仓混成统一剩余持有期。

报告中的 `n` 必须是交易事件数，不是调仓日数。

四关中的“前后半样本同向”必须按 `entry_date` 时间排序后切分；交易明细的
输入行序（包括按收益率排序的导出）不得影响 t、MDE、同向判定或最终结论。

**为什么必须配对**：我第一次做的零模型是错的——随机入场日 + 扫 1..10 的
argmax **恒等于 H=10**（300 次里 282 次），因为纯漂移让持有越久均值越大。
那个零模型测的是漂移不是过拟合。**配对差分才是对的比较。**

**实测**（796 笔，必须使用干净的 hfq_close 面板）：

| 比较 | Δmean | t | 判读 |
|---|---|---|---|
| H=3 − H=1 | +0.925% | **+3.93** | 显著更好 |
| H=3 − H=2 | +0.486% | **+3.17** | 显著更好 |
| H=3 − H=4 | +0.217% | +1.33 | 不可区分 |
| H=3 − H=6 | +0.125% | +0.42 | 不可区分 |
| H=3 − H=10 | +0.122% | +0.27 | 不可区分 |

这些数字不能用 `prices.csv` 这类已把买入日 close 覆盖成合成 open 的面板验收；
覆盖面板是缺陷①的产物，会系统性改变 H=1 等短持有期比较。覆盖面板不得作为验收基准。

分半（H=3 vs H=6）：前半 2020-2022 +0.265%（t=0.71）、
后半 2023-2026 +0.020%（t=0.04）。

**正确报文**：

> H=3 相对更短持有期（H=1、H=2）显著更好（t=3.93、3.17），
> 相对 H≥4 不可区分（|t|<1.4）。**H≥3 是平台不是峰** ——
> 你没有在持有期上过拟合，但也没有证据支持延长到 5 天以上。
> 这不是建议你换档。

**报文禁止事项**：不许出现「建议改为 H=X」「最优 H=X」；
不许把 argmax 当发现（[[extremal-bias-argmax-null]]）。

**本账验收断言（796 笔交易、干净 hfq_close 面板）**：H=3−H=1 的配对 t 约
**+3.93**，H=3−H=2 约 **+3.17**，H=3−H=4 的 `|t|<1.4`。若 `n` 等于
调仓日数（本账为 1134）或 t 接近 +0.50，说明仍在算调仓级组合，不得验收通过。

### 4.4 检查 ②：Top-N 邻域（宽度梯度，仅陈列）

扫 `N ∈ {1, 3, 5, 10, 20, 50}`（预声明网格，不许按结果增删），
每档报**几何**年化（不是算术），但本项只做固定网格陈列。

当前版本不对跨 N 差异作零模型、配对 t 检验或 MDE 判定；报告必须标为
`SKIP/仅陈列`，并明确这些数字不能被读成“某一档更优”或显著结论。

理由：[[extreme-tail-loses-breadth-gradient]] 实测净值符号是持仓只数的
函数（top-30 −21.34% / top-1000 +10.98%，跨 N 摆动 32pp），且算术复利
虚高 19pp。Top1 策略是最极端的窄口径 ⇒ 这一项必报。

本项需要用户提供**信号强度面板**（`date/code/score`）才能扫 N。
没有 ⇒ skip 并说明「缺信号面板，无法回答 N=1 是否是选出来的」。

### 4.5 检查 ③：入场时点邻域

`execution.entry` 在 `{open, close}` 间切换，配对检验。

**实测方向已知**：[[mined-alpha-lives-in-overnight-gap]] —— close-to-close
打分会定向偏好不可成交的隔夜段。本策略入场用 open 是**正确的一侧**，
应当明确报「你的入场口径避开了隔夜跳空，这是对的」。这是少见的正向确认。

### 4.6 四关闸门（沿用族六，但输出只到「体检结论」）

任何邻域档要被称为「显著不同」必须同时过：

1. 配对 t 检验 |t| ≥ 2；
2. 前后半样本同向；
3. 效应量超过本样本 MDE（`1.96 × SE`，报出来）；
4. 交易笔数 n ≥ 100。

全过 ⇒ 报「你这档与 X 档有实质差异」；否则 ⇒ 报「不可区分」。
**两种结论都不含建议。**

---

## 5. 族三/族五：推断项升级为事实（离线版）

### 5.1 现状：全靠 6 位代码猜板

`capacity.py:120` 的 `_board_limit` 按代码前缀给 10%/20%/30%，
自己声明「新股前 5 日无限幅、ST 是 5% —— 仍会误判」。

**实测误判量级**：抽 60 个买入日拉真 `up_limit` 对照，
**0 笔开盘即涨停**。工具报的「涨停要买 13 个标的-期」全是误报。

### 5.2 改法：可选输入列优先，猜板兜底

| 检查 | 有可选列时 | 无可选列时 |
|---|---|---|
| 不可成交权重 | 用 `up_limit`/`down_limit`/`is_suspended` ⇒ 标为【事实】 | 猜板 ⇒ 保留【推断】声明 |
| 缺价记账 | 用 `is_suspended` 区分停牌 vs 缺数 | 现有启发式 |
| 生存者偏差 | 用 `delisted=` 名单 ⇒ 可判定 | §3 密度门 + 「无法判定」 |

报文必须标明数据来源档位：**【事实：用户提供标志位】** 或
**【推断：按代码猜板，会误判】**。现在两者混在一个 WARN 里。

### 5.3 新增可选参数

```python
sa.audit(..., delisted=["000001", ...])          # 退市名单
sa.audit(..., signals=signal_panel)              # date/code/score，供 §4.4
sa.audit(..., trades=trade_details)              # entry_date/code[/exit_date]，供 §5.4
```

### 5.4 新增检查：跌停出场顺延（`deferred_exit`）

需要 `down_limit` 列 + 带 `exit_date` 的交易明细。回答：**出场有多少笔撞在
跌停封板日、顺延到次个非跌停日后差多少。**

**判据（不可协商）：市场 `close == down_limit`，绝不用成交价。**

E2E 第一版用 `实际卖出价 == down_limit` 判定，得 **0/796**，据此写下
「本 796 笔中未观察到收盘跌停卖出」。这个判据在构造上不可能命中——
成交价含滑点，永远不精确等于跌停价。换成市场行情价判据后实测：

| 项 | 实测 |
|---|---|
| 出场撞跌停封板 | **8/796**（收盘精确 −10.0%，close 精确等于 down_limit） |
| 这 8 笔平均毛收益 | **−7.47%**（全样本 +1.52%） |
| 合计占全样本总盈亏 | **−13.41%** |
| 顺延到次个非跌停日 | 平均 **−12.19%**，逐笔再差 **−4.72%** |
| 同一批笔用成交价判 | **0/796**（滑点使等式恒不成立） |

最极端一笔 002245 2024-11-26：市场收盘 10.78 = down_limit，而 xlsx
`实际卖出价` 7.2175 —— 比市场收盘低 33%，这本身值得单独问用户。

**判据错的方向是【漏报】**，也就是最不该出错的方向：它把有问题的样本
报成干净。所以本项的验收断言必须包含「用成交价判必然得 0」这个反例护栏。

**附带硬要求**：涨跌停价必须与 close 同复权基准。两边基准不同会让
`close == down_limit` 恒不成立，是同一类静默漏报。

**入场价口径必须跟随 `execution`**：本项报的毛收益若用 close 当入场价，
会与 §1/§5 那套 open 入场的数字对不上，同一份报告出现两个矛盾均值
（实测 −10.22% vs −7.47%）。

**报文立场**：只报事实与顺延量级，不建议改策略。顺延方向不确定
（次日可能继续跌也可能回升），怎么处理是用户的决定。

### 5.5 离群笔必须归因，不能只写「须追溯」

判别法：若**未复权**比值与用户数字精确相等（|diff| ≤ 1e-9）而后复权
比值不等，则差异全部来自两家的**复权基准**，不是价格错。

实测 603501 2021-01-06→01-08：后复权差 2.00 bp，未复权比值精确相等，
`adj_factor` 在持有期内从 1.0048 变到 1.0050 ⇒ 复权基准差异。
结论是「无需向数据商追价格，但跨供应商比较后复权收益时必须固定同一
复权基准」，比「须追溯」可执行得多。

---

## 6. 供应商鲁棒性（离线版，**已降级**）

### 6.1 必须说清的降级

原设想是「工具用通用接口独立重选信号，配对检验 edge 是否可移植」。
**离线口径下这半句做不了** —— 工具不取外部数据，就没有第二个信号源。

我实测过联网版能给出什么，记录在此供将来参考：

- 从用户表里的 `主力净流入(亿元)` 数值反解定义：`elg+lg`（大单+超大单
  净额）中位相对误差 **2.8%**，其余候选 21%/47%/68% ⇒ 定义可锁定；
- 但只有 56% 的笔落在 5% 误差内 ⇒ 他的数据商 ≠ tushare；
- 120 个信号日独立重选 Top1，**仅 24% 同票**；
- 103 笔配对：用户 Top1 +1.960%（胜率 59.2%，t=2.70）、
  我的 Top1 +1.838%（胜率 47.6%，t=1.77）、**配对差 −0.122%（t=−0.11）**
  ⇒ **他的 edge 不依赖他的数据商**。

这是个可写进募资材料的结论，也能反着用（配对差显著为负 ⇒ alpha 是
数据商口径伪影）。**离线版拿不到。**

### 6.2 离线版能做的两件事

**① 自洽性检查（不需要第二家数据）**

用户提供信号面板 `signals`（`date/code/score`）时，检查
「他的面板能不能重现他自己的选票」：

```
对每个调仓日：argmax_code(score) 是否等于 weights 里实际买入的 code
```

不一致 ⇒ 抓到**选股实现 bug**（面板与下单不一致、排序方向反了、
tie 处理不同）。这条抓的是 [[rank-qcut-direction-and-tie-traps]] 那类错误，
而且完全离线。

**实测预期**：这份表若补上信号面板，一致率应接近 100%（因为表里
`主力净流入` 与 `证券代码` 同行），偏离即为 bug。

**② 跨供应商配对检验（用户自带第二份）**

```python
sa.audit(..., signals=panel_a, signals_alt=panel_b)
```

两个面板各自选 Top-N，在**同一价格面板**上配对检验收益差。
过 §4.6 四关才报结论。用户没有第二份 ⇒ skip 并说明这一项的价值：

> 缺第二家供应商的信号面板。补上可回答「你的 alpha 是策略的还是
> 数据商口径的」—— 这是本工具能给出的最强单条结论，但需要你自己
> 提供第二份数据。

---

## 7. 能力矩阵更新

新增输入种类：

```python
OPEN = "open"          # 价格面板的 open 列
FLAGS = "flags"        # up_limit/down_limit/is_suspended/is_st
SIG = "signals"        # 信号强度面板
SIG_ALT = "signals_alt"  # 第二家供应商信号面板
DELISTED = "delisted"  # 退市名单
PARAMS = "params"      # 用户声明的参数
```

检查项从 26 → 30：

| 新项 | 族 | 需要 |
|---|---|---|
| 持有期邻域体检 | 七 | W + P + `params.holding_days` |
| Top-N 邻域体检 | 七 | W + P + SIG + `params.top_n` |
| 入场时点体检 | 七 | W + P + OPEN |
| 信号-持仓自洽 | 七 | W + SIG |

外加 §6.2② 跨供应商配对（需 SIG + SIG_ALT）⇒ 31 项。

**能力矩阵必须显示**：这份表（无 SIG、无 FLAGS、无 params）
只能多审 0 项 —— 也就是说**族七对纯平台导出件不产出任何东西**。
这一点必须诚实打印，不许把「用户没给输入」显示成「策略没问题」。

---

## 8. 明确不做

- **不推荐更优参数/更优策略。** 依据 `PRESCRIBE_CALIBRATION.md` §①②③ 的
  四个失败构造。族七只体检。
- **不内置数据源、不联网、不上传。** README 承诺不变。
- **不从数据反推用户没声明的参数再扫。** 那是拿结果选搜索空间。
- **不做因子级前视。** 需要策略源码，数据审计抓不到（v0.1 报告已声明）。
- **不预测未来表现。**

---

## 9. 验收

### 9.1 恒等式护栏（最高优先级）

```python
def test_identity_reported_as_skip():
    """自报净值与重算同源时，对账项必须 SKIP 而非 OK。"""
    # 构造：nav 由 weights+prices 精确算出
    rep = sa.audit(w, p, nav_exact, own_nav=nav_exact)
    f = find(rep, key="nav_recon")
    assert f.level == "SKIP"
    assert "不可审" in f.detail and "同源" in f.detail
```

### 9.2 稀疏面板护栏

```python
def test_sparse_panel_skips_survivorship():
    """末日覆盖率 < 50% 时不许输出「已退市」结论。"""
    rep = sa.audit(w, sparse_p)
    assert any("末日价格覆盖率" in s for s in rep.skipped)
    assert not any("已退市" in f.detail for f in rep.findings)
```

### 9.3 族七不给建议

```python
def test_family7_never_recommends():
    rep = sa.audit(w, p, params={"holding_days": 3})
    for f in family7(rep):
        for banned in ("建议改为", "最优", "推荐使用", "应当改成"):
            assert banned not in f.detail + f.impact
```

### 9.4 真实数据回归

把这份客户件封成回归案例（`tests/test_real_data_lessons.py` 已有先例）：

- `execution={"entry":"open"}` + 真 open 列 ⇒ 对账不再是恒等式；
- 完整日频面板 ⇒ 生存者报「无法判定」不报「313 只已退市」；
- `params={"holding_days":3}` ⇒ 族七报「H≥3 是平台」且不含建议词；
- 无 SIG/FLAGS ⇒ 能力矩阵显示族七大部分不可审。
- 交易级持有期回归：本账 `n=796`，H=3 vs H=1 的 `t≈+3.93`，H=3 vs H=2
  的 `t≈+3.17`（干净 hfq_close 面板）；若 `n=1134` 或 `t≈+0.50`，判定为
  调仓级实现错误。另须断言同一批 trades 的 `exit_date` 与 H=holding_days
  （H 含入场日）在可比事件上逐笔一致。
- 行序不变量：将同一份 `trades` 任意打乱行序后，持有期配对的 t、MDE、
  前后半同向和“实质差异/不可区分”结论必须不变；前后半按 `entry_date` 切分。
- Top-N 回归必须是 `SKIP/仅陈列`，报文含“没有零模型、配对 t 检验或 MDE”。
- 带真实 `open` 列的执行回归：同一权重/close 面板下，`entry="open"` 的重算
  收益必须与默认 close 入场不同；只验证缺列不回退不算完成。

### 9.5 数字对账

`tests/test_readme_numbers.py` 同款：本 SPEC 引用的每个实测数字
（784/796、5.6e-17、0.0000、1/314、+3.93、+3.13、0/60、24%、−0.11）
必须有脚本可复现，或标注为一次性探针结果。

---

## 10. 顺手要修的

`D:/desk/strategy_audit_inputs/fetch_prices.py:23` 硬编码了 tushare token
明文。建议挪进环境变量或凭据管理器（参照 [[pypi-token-location]]）。
该文件不属于本包，但它是这次审计留下的产物。
